# neural-network-from-scratch
Built a neural network from scratch in Python for MNIST handwritten digit classification, implementing forward propagation, backpropagation, activation functions, loss functions, and gradient descent without TensorFlow or Keras
